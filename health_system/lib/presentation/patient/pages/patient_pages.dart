import 'package:flutter/material.dart';

class PatientPages extends StatelessWidget {
  const PatientPages({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Patient'),
        elevation: 0,
      ),
    );
  }
}

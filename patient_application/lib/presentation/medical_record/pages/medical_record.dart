import 'package:flutter/material.dart';

class MedicalRecords extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Medical Records Page Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
